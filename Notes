/home/nix/u01/blockchain-integration/scripts/restart-network.sh
/home/nix/u01/blockchain-integration/scripts/start-network.sh
/home/nix/u01/blockchain-integration/scripts/stop-network.sh
/home/nix/u01/blockchain-integration/scripts/verify-network.sh

1. Verify Docker service
sudo systemctl status docker


nix@pg-replica:~/u01/blockchain-integration$ docker network create blockchain-net
affc5abb78d82b151a8d1251509f44d333fde027d57e7ddc80d262e0a6b63603


Create network
~/u01/blockchain-integration/docker/networks/create-networks.sh

Create Docker Verification Script
~/u01/blockchain-integration/docker/scripts/docker-check.sh


Docker Commands Required for Hyperledger Fabric:

From Fabric test network folder:
cd ~/u01/blockchain-integration/fabric/fabric-samples/test-network

Check Fabric containers:
docker ps

Check all containers:
docker ps -a

Check Fabric images:
docker images | grep hyperledger

Check CouchDB containers:
docker ps | grep couchdb

View peer logs:
docker logs peer0.org1.example.com

View orderer logs:
docker logs orderer.example.com

View CouchDB logs:
docker logs couchdb0



Final Verification Checklist
docker version
docker compose version
docker info
docker run hello-world
docker network ls
docker ps
systemctl status docker
systemctl status containerd


step 5
Main Commands
Run Docker verification
~/u01/blockchain-integration/docker/scripts/docker-check.sh
Create Docker networks
~/u01/blockchain-integration/docker/networks/create-networks.sh
Restart Docker runtime after server timeout
~/u01/blockchain-integration/docker/scripts/restart-containers.sh
View container logs
~/u01/blockchain-integration/docker/scripts/view-logs.sh <container_name>

Example:

~/u01/blockchain-integration/docker/scripts/view-logs.sh peer0.org1.example.com
Clean unused Docker resources
~/u01/blockchain-integration/docker/scripts/docker-clean.sh
Important Notes

Use:

docker compose

Do not use old:

docker-compose

Do not delete Docker volumes unless you are sure they do not contain PostgreSQL, CouchDB, or Fabric ledger data.
EOF

echo "Created:"
echo "$DOCKER_DIR/README.md"

echo ""




Verify CouchDB DNS from Docker network
docker run --rm --network blockchain_net busybox nslookup couchdb0.org1
docker run --rm --network blockchain_net busybox nslookup couchdb0.org2

